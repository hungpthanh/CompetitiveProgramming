# Idea: TOC - Math
# for each point, check all points same row and same col and check the
# four point. Except these 4 points checking any point inside
class Solution:
    def maxRectangleArea(self, points: List[List[int]]) -> int:
        